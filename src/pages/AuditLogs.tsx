import { useQuery } from "@tanstack/react-query";
import { History } from "lucide-react";
import { apiFetch } from "../lib/api";
import type { AuditLog } from "../types";
export function AuditLogs() { const query = useQuery({ queryKey: ["audit-logs"], queryFn: () => apiFetch<{ logs: AuditLog[] }>("/api/admin/audit-logs") }); return <div><div><p className="text-sm font-bold uppercase tracking-[.18em] text-pine">Administration</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Audit logs</h1><p className="mt-2 text-ink/55">Trace important changes across the business system.</p></div><div className="mt-6 overflow-hidden rounded-3xl bg-white shadow-sm">{query.isLoading ? <div className="p-8 text-center text-sm text-ink/50">Loading audit logs…</div> : query.data?.logs.map((log) => <div key={log.id} className="flex flex-col gap-3 border-b border-black/5 px-5 py-5 last:border-0 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-lime text-pine"><History size={18} /></div><div><p className="font-semibold">{log.action.replaceAll("_", " ")}</p><p className="text-sm text-ink/50">{log.entity}{log.entityId ? ` · ${log.entityId}` : ""}</p></div></div><div className="text-left text-sm text-ink/50 sm:text-right"><p>{log.user ? `${log.user.firstName} ${log.user.lastName}` : "System"}</p><p className="text-xs">{new Date(log.createdAt).toLocaleString()}</p></div></div>)}</div></div>; }

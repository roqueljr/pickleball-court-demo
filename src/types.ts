export type Role = "SUPER_ADMIN" | "ADMIN" | "STAFF" | "COACH" | "CUSTOMER";
export type User = { id: string; email: string; firstName: string; lastName: string; phone: string | null; emailVerified: boolean; roles: Role[] };
export type Court = { id: string; name: string; description: string | null; location: string | null; indoor: boolean; surfaceType: string | null; status: string; openingTime: string; closingTime: string; hourlyRate: number; imageUrl: string | null; features: unknown };
export type Booking = { id: string; reference: string; startsAt: string; endsAt: string; durationMinutes: number; status: string; subtotal: number; discount: number; tax: number; total: number; promoCode: string | null; qrToken?: string; canReschedule?: boolean; rescheduleDeadline?: string | null; accessPass?: { id: string; status: string; validFrom: string; validUntil: string; usedAt: string | null } | null; court: Court; customer?: { user: { firstName: string; lastName: string; email: string } }; payments?: { id: string; finalAmount: number; status: string; method: string }[] };
export type Customer = { id: string; firstName: string; lastName: string; email: string; phone: string | null; address: string | null; notes: string | null; status: string; registeredAt: string; membership: { id: string; plan: { name: string; discountPercent: number }; endDate: string } | null; bookingsCount: number; paymentsCount: number };
export type Payment = { id: string; bookingId: string | null; membershipId?: string | null; amount: number; discount: number; tax: number; finalAmount: number; refundedAmount: number; method: string; status: string; transactionReference: string | null; paidAt: string | null; createdAt: string; booking: { reference: string; status: string; court: { name: string } } | null; membership?: { plan: { name: string } } | null; packagePurchase?: { packagePlan: { name: string } } | null; openPlayParticipant?: { openPlay: { title: string } } | null; leagueEntry?: { league: { name: string } } | null; coachingSession?: { status: string; coach: { user: { firstName: string; lastName: string } } } | null; walletTopUp?: { amount: number } | null; customer: { user: { firstName: string; lastName: string; email: string } } };
export type MembershipPlan = { id: string; name: string; description: string | null; price: number; durationDays: number; discountPercent: number; maximumBookings: number | null; benefits: string[]; isActive: boolean };
export type Membership = { id: string; startDate: string; endDate: string; status: string; plan: { id: string; name: string; price: number; discountPercent: number; durationDays?: number; maximumBookings?: number | null; benefits: string[] } };
export type Promotion = { id: string; code: string; discountPercent: number | null; fixedDiscount: number | null; startDate: string; endDate: string; usageLimit: number | null; usedCount: number; minimumPurchase: number; applicableCourtIds: string[]; applicablePlanIds: string[]; isActive: boolean };
export type Coach = { id: string; biography: string | null; experience: string | null; certifications: string | null; hourlyRate: number; status: string; user: { id?: string; firstName: string; lastName: string; email: string } };
export type CoachingSession = { id: string; startsAt: string; endsAt: string; rate: number; status: string; notes: string | null; coach: { user: { firstName: string; lastName: string } }; customer: { user: { firstName: string; lastName: string; email: string } }; court: Court | null };
export type Equipment = { id: string; name: string; quantity: number; availableQuantity: number; rentalPrice: number; status: string };
export type EquipmentRental = { id: string; equipmentId: string; bookingId: string | null; quantity: number; unitPrice: number; startsAt: string; endsAt: string; status: "RESERVED" | "RETURNED" | "CANCELLED"; returnedAt: string | null; equipment: Equipment; customer: { user: { firstName: string; lastName: string } } };
export type Product = { id: string; sku: string; name: string; categoryId?: string | null; description: string | null; price: number; cost: number; stock: number; lowStockThreshold: number; status: string; category: { name: string } | null };
export type Expense = { id: string; category: string; description: string; amount: number; date: string; paymentMethod: string; receiptRef: string | null; createdBy: { firstName: string; lastName: string } };
export type Notification = { id: string; type: string; title: string; message: string; readAt: string | null; createdAt: string; actionUrl?: string };
export type ReportSummary = { currency: string; revenue: { payments: number; bookings: number; memberships: number; coaching: number; pos: number; rentals: number; packages: number; openPlay: number; leagues: number; walletTopUps: number; other: number; total: number }; expenses: number; netProfit: number; bookingCount: number; bookingStatuses: { status: string; _count: { _all: number } }[]; courtHours: { court: string; bookedHours: number; availableHours: number; utilizationPercent: number }[] };
export type AdminUser = { id: string; email: string; firstName: string; lastName: string; phone: string | null; status: string; createdAt: string; roles: Role[]; profile: string | null };
export type AuditLog = { id: string; action: string; entity: string; entityId: string | null; metadata: unknown; createdAt: string; user: { firstName: string; lastName: string; email: string } | null };
export type AdminDashboard = { stats: { bookings: number; revenue: number; customers: number; availableCourts: number; occupiedCourts: number; cancelled: number }; courts: { id: string; name: string; status: string; occupied: boolean }[]; recentBookings: { id: string; reference: string; status: string; total: number; startsAt: string; court: { name: string }; customer: { firstName: string; lastName: string } | null }[]; recentCustomers: { id: string; name: string; email: string; createdAt: string }[] };
export type Tournament = { id: string; title: string; slug: string; description: string; format: string; registrationMode: string; skillLevel: string; startsAt: string; endsAt: string; registrationDeadline: string; location: string; entryFee: number; maxRegistrations: number | null; teamSize: number; imageUrl: string | null; featured: boolean; status: string; registrationsCount: number };
export type TournamentRegistration = { id: string; mode: string; teamName: string | null; captainName: string; captainEmail: string; captainPhone: string; notes: string | null; status: string; participants: { id: string; fullName: string; email: string; phone: string | null }[]; createdAt: string };
export type OpenPlayParticipant = { id: string; status: string; amount: number; paymentMethod: string; paidAt: string | null; customer: { id: string; skillRating: number; user: { firstName: string; lastName: string } } };
export type OpenPlay = { id: string; title: string; startsAt: string; endsAt: string; skillMin: number; skillMax: number; capacity: number; pricePerPlayer: number; competitive: boolean; status: string; notes: string | null; court: Court; participants: OpenPlayParticipant[] };
export type WaitlistEntry = { id: string; startsAt: string; durationMinutes: number; status: string; offerExpiresAt: string | null; court: Court; claimedBooking?: { reference: string } | null; customer?: { user: { firstName: string; lastName: string; email: string } } };
export type PackagePlan = { id: string; name: string; description: string | null; price: number; bookingCredits: number; validityDays: number; isActive: boolean };
export type PackagePurchase = { id: string; creditsRemaining: number; startsAt: string; expiresAt: string; status: string; packagePlan: PackagePlan; payments: { id: string; status: string; finalAmount: number }[] };
export type LeagueEntry = { id: string; status: string; wins: number; losses: number; points: number; customer: { id: string; skillRating: number; user: { firstName: string; lastName: string } } };
export type LeagueMatch = { id: string; homeEntryId: string; awayEntryId: string; scheduledAt: string; homeScore: number | null; awayScore: number | null; winnerEntryId: string | null; status: string };
export type League = { id: string; name: string; description: string | null; skillMin: number; skillMax: number; startsAt: string; endsAt: string; maxPlayers: number | null; entryFee: number; status: string; entries: LeagueEntry[]; matches: LeagueMatch[] };
export type PricingRule = { id: string; name: string; courtId: string | null; weekday: number | null; startTime: string; endTime: string; adjustmentPercent: number; minimumLeadHours: number | null; maximumLeadHours: number | null; isActive: boolean; court: { name: string } | null };
export type AutomationCampaign = { id: string; name: string; kind: string; status: string; subject: string; message: string; actionUrl: string | null; triggerDays: number; lastRunAt: string | null; runs: { id: string; recipients: number; createdAt: string }[] };
export type GrowthInsights = { metrics: { waitlistRecoveredRevenue: number; waitlistWaiting: number; openPlayRevenue: number; dormantCustomers: number; expiringMemberships: number; guestLeads: number; walletLiability: number }; utilization: { courtId: string; court: string; bookedHours: number; availableHours: number; utilizationPercent: number }[]; recommendations: { type: string; title: string; message: string; actionUrl: string }[] };
